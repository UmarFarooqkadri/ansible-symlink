---

- hosts: localhost
  vars:
    folderpath:
      dev/env1/prodname1
      dev/env2/prodname2
  tasks:
  - name: Createa symlink
    shell: "echo {{folderpath}} | awk -F'/' '{system(\"mkdir \" $2_$3)}'"

    # command: "mkdir /home/ubuntu/playbook/{{env}}_{{product}}"
    with_items:
      - " {{folderpath}} "
